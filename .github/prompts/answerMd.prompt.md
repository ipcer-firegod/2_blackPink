---
agent: 'ask'
description: '回答问题时，输出层次化的 Markdown（中文），不要只返回单一大标题。'
---

请完成以下要求：

- 语言：中文（简体）。
- 目标：当问题时，输出一个结构化、可直接复制为 Markdown 文件的回答（不要只返回单一的 `#` 或 `##` 标题）。

必须包含的结构（建议顺序且可按需省略不相关部分）：

1. `# 标题`（一行概述）
2. `> 摘要`（一段，不超过三行）
3. 后续其他md格式段落，按需分段，使用 `##`、`###` 等标题层级组织内容